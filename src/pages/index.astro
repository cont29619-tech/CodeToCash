---
import Layout from '../layouts/Layout.astro';

const schema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "CodeToCash.dev",
  "url": "https://codetocash.dev",
  "description": "Direct response marketing education for vibe coders and indie developers who ship products but struggle with sales.",
  "potentialAction": {
    "@type": "SearchAction",
    "target": "https://codetocash.dev/?q={search_term_string}",
    "query-input": "required name=search_term_string"
  }
});
---

<Layout schema={schema}>

  <!-- ============ HERO SECTION ============ -->
  <section class="relative overflow-hidden min-h-[90vh] flex items-center justify-center px-4 py-24">
    <!-- Background glow (simplified on mobile for faster paint) -->
    <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[600px] bg-coral/5 rounded-full blur-[60px] md:blur-[120px] animate-pulse-slow"></div>
      <div class="absolute top-1/3 left-1/3 w-[400px] h-[400px] bg-deep-blue/10 rounded-full hidden md:block blur-[100px] animate-pulse-slow animation-delay-2000"></div>
    </div>
    <!-- Faded code decoration (loaded after paint to avoid LCP penalty) -->
    <div id="code-decoration" class="absolute inset-0 pointer-events-none overflow-hidden opacity-0 select-none" aria-hidden="true">
    </div>

    <div class="relative z-10 max-w-4xl mx-auto text-center">
      <p class="inline-block text-sm font-mono text-coral uppercase tracking-widest mb-6 animate-fade-in">// ship products, make money</p>
      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-white leading-[1.1] tracking-tight animate-fade-in-up">
        You Can Build Anything.<br />
        <span class="text-coral">Now Learn to Sell It.</span>
      </h1>
      <p class="mt-6 text-lg sm:text-xl text-gray-400 max-w-2xl mx-auto leading-relaxed animate-fade-in-up animation-delay-200">
        Direct response marketing frameworks for vibe coders and indie developers who ship products but struggle with sales.
      </p>
      <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in-up animation-delay-400">
        <a
          href="/drm-101"
          class="px-8 py-4 bg-coral text-white text-lg font-semibold rounded-lg hover:bg-coral/90 hover:scale-[1.02] transition-all duration-200 shadow-lg shadow-coral/20"
        >
          Start the Free DRM 101 Guide
        </a>
        <a
          href="/newsletter"
          class="px-8 py-4 border border-white/20 text-white text-lg font-medium rounded-lg hover:border-coral/50 hover:text-coral transition-all duration-200"
        >
          Join 2,000+ devs learning to sell
        </a>
      </div>
    </div>
  </section>


  <!-- ============ PROBLEM SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32 bg-navy-light/50">
    <div class="max-w-6xl mx-auto">
      <div class="text-center max-w-3xl mx-auto mb-16 section-animate">
        <p class="text-sm font-mono text-coral uppercase tracking-widest mb-4">// the problem</p>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
          The Vibe Coder's Marketing Problem
        </h2>
        <p class="mt-6 text-lg text-gray-400 leading-relaxed">
          You can vibe code an entire SaaS in a weekend with Cursor, Bolt, or Lovable. But then what? You post it on Twitter, get 12 likes, and wonder why nobody is buying.
        </p>
        <p class="mt-4 text-lg text-gray-400 leading-relaxed">
          The problem isn't your product — it's that <span class="text-white font-medium">nobody taught you how to sell</span>. Traditional marketing advice feels like it's written for MBAs, not makers.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] transition-all duration-300 section-animate">
          <div class="text-4xl mb-5">🚀</div>
          <h3 class="text-xl font-bold text-white mb-3">You Build, But Nobody Buys</h3>
          <p class="text-gray-400 leading-relaxed">Great product, zero revenue. You shipped something amazing but your launch got buried in the noise.</p>
        </div>
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] transition-all duration-300 section-animate">
          <div class="text-4xl mb-5">😬</div>
          <h3 class="text-xl font-bold text-white mb-3">Marketing Feels Sleazy</h3>
          <p class="text-gray-400 leading-relaxed">You'd rather write code than write copy. Every marketing tactic you've seen feels manipulative or cringe.</p>
        </div>
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] transition-all duration-300 section-animate">
          <div class="text-4xl mb-5">🎲</div>
          <h3 class="text-xl font-bold text-white mb-3">No System, Just Hoping</h3>
          <p class="text-gray-400 leading-relaxed">Posting randomly and praying for sales. No funnel, no strategy, no repeatable process to grow revenue.</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ============ SOLUTION SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32">
    <div class="max-w-6xl mx-auto">
      <div class="text-center max-w-3xl mx-auto mb-16 section-animate">
        <p class="text-sm font-mono text-coral uppercase tracking-widest mb-4">// the solution</p>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
          Direct Response Marketing:<br />Your Sales Algorithm
        </h2>
        <p class="mt-6 text-lg text-gray-400 leading-relaxed">
          Think of DRM as a function. <span class="font-mono text-coral/80">Input: visitor</span>. <span class="font-mono text-coral/80">Output: customer</span>. Every element is testable. Every result is measurable. It's marketing for people who think in systems.
        </p>
      </div>

      <!-- DRM Pipeline -->
      <div class="max-w-4xl mx-auto section-animate">
        <div class="rounded-2xl bg-white/[0.02] border border-white/[0.06] overflow-hidden">
          <!-- Terminal header -->
          <div class="flex items-center gap-2 px-5 py-3 bg-white/[0.03] border-b border-white/[0.06]">
            <span class="w-3 h-3 rounded-full bg-coral/60"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-500/60"></span>
            <span class="w-3 h-3 rounded-full bg-green-500/60"></span>
            <span class="ml-3 text-xs font-mono text-gray-500">drm-pipeline.ts</span>
          </div>
          <!-- Terminal body -->
          <div class="p-6 sm:p-8 font-mono text-sm sm:text-base leading-loose">
            <p class="text-gray-500">// Your direct response marketing pipeline</p>
            <p class="text-gray-500">// Every step is measurable, every result is optimizable</p>
            <p class="mt-4">
              <span class="text-purple-400">const</span>
              <span class="text-white"> pipeline</span>
              <span class="text-coral"> =</span>
              <span class="text-white"> [</span>
            </p>
            <div class="ml-4 sm:ml-8 space-y-3 my-3">
              <p>
                <span class="text-green-400">"Traffic"</span><span class="text-gray-500">,</span>
                <span class="text-gray-600 ml-6">// SEO, ads, content, social</span>
              </p>
              <p>
                <span class="text-green-400">"Landing Page"</span><span class="text-gray-500">,</span>
                <span class="text-gray-600 ml-3">// Convert visitors to leads</span>
              </p>
              <p>
                <span class="text-green-400">"Email Sequence"</span><span class="text-gray-500">,</span>
                <span class="text-gray-600 ml-1">// Nurture &amp; build trust</span>
              </p>
              <p>
                <span class="text-green-400">"Offer"</span><span class="text-gray-500">,</span>
                <span class="text-gray-600 ml-10">// Present irresistible deal</span>
              </p>
              <p>
                <span class="text-green-400">"Revenue"</span><span class="text-gray-500">,</span>
                <span class="text-gray-600 ml-7">// $$$</span>
              </p>
            </div>
            <p><span class="text-white">];</span></p>
            <p class="mt-4">
              <span class="text-purple-400">const</span>
              <span class="text-white"> result</span>
              <span class="text-coral"> =</span>
              <span class="text-yellow-300"> runPipeline</span><span class="text-white">(pipeline);</span>
            </p>
            <p>
              <span class="text-gray-500">// =&gt; </span>
              <span class="text-coral font-bold">&#123; revenue: "recurring", confidence: "data-driven" &#125;</span>
            </p>
          </div>
        </div>

        <!-- Pipeline visual (mobile-friendly) -->
        <div class="mt-10 flex flex-wrap items-center justify-center gap-3 text-sm font-mono section-animate">
          <span class="px-4 py-2 rounded-lg bg-deep-blue/40 border border-deep-blue/60 text-gray-200">Traffic</span>
          <span class="text-coral text-lg">→</span>
          <span class="px-4 py-2 rounded-lg bg-deep-blue/40 border border-deep-blue/60 text-gray-200">Landing Page</span>
          <span class="text-coral text-lg">→</span>
          <span class="px-4 py-2 rounded-lg bg-deep-blue/40 border border-deep-blue/60 text-gray-200">Email Sequence</span>
          <span class="text-coral text-lg">→</span>
          <span class="px-4 py-2 rounded-lg bg-deep-blue/40 border border-deep-blue/60 text-gray-200">Offer</span>
          <span class="text-coral text-lg">→</span>
          <span class="px-4 py-2 rounded-lg bg-coral/20 border border-coral/40 text-coral font-semibold">Revenue</span>
        </div>
      </div>
    </div>
  </section>


  <!-- ============ WHAT YOU'LL LEARN SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32 bg-navy-light/50">
    <div class="max-w-6xl mx-auto">
      <div class="text-center max-w-3xl mx-auto mb-16 section-animate">
        <p class="text-sm font-mono text-coral uppercase tracking-widest mb-4">// the curriculum</p>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
          What You'll Learn at CodeToCash
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <!-- Card 1 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2" /><path d="M3 9h18" /><path d="M9 21V9" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Landing Pages That Convert</h3>
          <p class="text-gray-400 text-sm leading-relaxed">Turn visitors into customers with proven page structures and conversion-optimized layouts.</p>
        </div>
        <!-- Card 2 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path d="M12 20h9" /><path d="M16.376 3.622a1 1 0 0 1 3.002 3.002L7.368 18.635a2 2 0 0 1-.855.506l-2.872.838a.5.5 0 0 1-.62-.62l.838-2.872a2 2 0 0 1 .506-.854z" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Copywriting for Developers</h3>
          <p class="text-gray-400 text-sm leading-relaxed">Frameworks that make writing sales copy feel like filling in a template. No creative writing degree needed.</p>
        </div>
        <!-- Card 3 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2" /><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Email Sequences That Sell</h3>
          <p class="text-gray-400 text-sm leading-relaxed">Automated email flows that nurture leads and drive revenue while you sleep. Set up once, profit forever.</p>
        </div>
        <!-- Card 4 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Your First Ad Campaign</h3>
          <p class="text-gray-400 text-sm leading-relaxed">Run paid ads without burning cash, even on a tiny budget. Learn to test, measure, and scale what works.</p>
        </div>
        <!-- Card 5 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><line x1="12" y1="1" x2="12" y2="23" /><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Pricing Strategies</h3>
          <p class="text-gray-400 text-sm leading-relaxed">Price your SaaS, tool, or template to maximize revenue. Stop guessing and start using proven pricing models.</p>
        </div>
        <!-- Card 6 -->
        <div class="group p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] hover:border-coral/20 hover:bg-white/[0.04] hover:-translate-y-1 transition-all duration-300 section-animate">
          <div class="w-12 h-12 rounded-xl bg-coral/10 flex items-center justify-center mb-5">
            <svg class="w-6 h-6 text-coral" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path d="M3 3v16a2 2 0 0 0 2 2h16" /><path d="m19 9-5 5-4-4-3 3" /></svg>
          </div>
          <h3 class="text-lg font-bold text-white mb-2">Analytics & Testing</h3>
          <p class="text-gray-400 text-sm leading-relaxed">A/B test everything and make data-driven marketing decisions. Because guessing is not a growth strategy.</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ============ SOCIAL PROOF SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32">
    <div class="max-w-6xl mx-auto">
      <div class="text-center max-w-3xl mx-auto mb-16 section-animate">
        <p class="text-sm font-mono text-coral uppercase tracking-widest mb-4">// social proof</p>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
          Built for Developers Like You
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] section-animate">
          <div class="flex gap-1 mb-5">
            <span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span>
          </div>
          <blockquote class="text-gray-300 leading-relaxed mb-6">
            "I went from 0 to $2K MRR in 3 months using the DRM frameworks from CodeToCash."
          </blockquote>
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-coral/20 flex items-center justify-center text-coral font-bold text-sm">A</div>
            <div>
              <p class="text-white font-medium text-sm">Alex</p>
              <p class="text-gray-500 text-xs">Indie Developer</p>
            </div>
          </div>
        </div>

        <div class="p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] section-animate">
          <div class="flex gap-1 mb-5">
            <span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span>
          </div>
          <blockquote class="text-gray-300 leading-relaxed mb-6">
            "Finally, marketing advice that doesn't make me cringe. It's like reading documentation, not a sales pitch."
          </blockquote>
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-deep-blue/40 flex items-center justify-center text-blue-300 font-bold text-sm">S</div>
            <div>
              <p class="text-white font-medium text-sm">Sam</p>
              <p class="text-gray-500 text-xs">Vibe Coder</p>
            </div>
          </div>
        </div>

        <div class="p-8 rounded-2xl bg-white/[0.02] border border-white/[0.06] section-animate">
          <div class="flex gap-1 mb-5">
            <span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span><span class="text-coral">★</span>
          </div>
          <blockquote class="text-gray-300 leading-relaxed mb-6">
            "The email sequence templates alone were worth it. Set it up once, sales come in on autopilot."
          </blockquote>
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-coral/20 flex items-center justify-center text-coral font-bold text-sm">J</div>
            <div>
              <p class="text-white font-medium text-sm">Jordan</p>
              <p class="text-gray-500 text-xs">SaaS Founder</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- ============ NEWSLETTER SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32 overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 bg-gradient-to-br from-deep-blue/30 via-navy-light to-coral/5 pointer-events-none" aria-hidden="true"></div>
    <div class="absolute inset-0 bg-navy-light/60 pointer-events-none" aria-hidden="true"></div>

    <div class="relative z-10 max-w-2xl mx-auto text-center section-animate">
      <p class="text-sm font-mono text-coral uppercase tracking-widest mb-4">// subscribe</p>
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
        The Dev Marketing Newsletter
      </h2>
      <p class="mt-6 text-lg text-gray-400 leading-relaxed">
        One actionable DRM tactic for vibe coders, every week. No fluff. No guru vibes. Just systems that sell.
      </p>

      <form class="mt-10 flex flex-col sm:flex-row gap-3 max-w-lg mx-auto">
        <input
          type="email"
          placeholder="you@email.com"
          aria-label="Email address"
          class="flex-1 min-w-0 px-5 py-4 bg-white/5 border border-white/10 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-coral/50 focus:ring-2 focus:ring-coral/20 transition-all duration-200"
        />
        <button
          type="submit"
          class="px-8 py-4 bg-coral text-white font-semibold rounded-lg hover:bg-coral/90 hover:scale-[1.02] transition-all duration-200 shadow-lg shadow-coral/20 shrink-0"
        >
          Subscribe
        </button>
      </form>

      <p class="mt-5 text-sm text-gray-500">
        Join free. Unsubscribe anytime. Get <span class="text-gray-300">The Developer's DRM Cheatsheet</span> instantly.
      </p>
    </div>
  </section>


  <!-- ============ FINAL CTA SECTION ============ -->
  <section class="relative px-4 py-24 sm:py-32">
    <div class="max-w-3xl mx-auto text-center section-animate">
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white leading-tight">
        Ready to turn your code<br />into <span class="text-coral">cash</span>?
      </h2>
      <a
        href="/drm-101"
        class="mt-10 inline-flex items-center px-10 py-4 bg-coral text-white text-lg font-semibold rounded-lg hover:bg-coral/90 hover:scale-[1.02] transition-all duration-200 shadow-lg shadow-coral/20"
      >
        Start the Free DRM 101 Guide
      </a>
    </div>
  </section>

</Layout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );

  document.querySelectorAll('.section-animate').forEach((el) => {
    observer.observe(el);
  });

  // Inject code decoration after paint so h1 is the LCP element
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const el = document.getElementById('code-decoration');
      if (el) {
        el.innerHTML = `<pre class="font-mono text-xs sm:text-sm text-white leading-relaxed absolute top-8 -left-8 rotate-[-3deg]">const pipeline = async (visitor) =&gt; {
  const lead = await captureEmail(visitor);
  const qualified = await nurture(lead);
  const customer = await presentOffer(qualified);
  return customer.revenue;
};

function buildLandingPage(product) {
  return {
    headline: solveMainProblem(product),
    subhead: agitate(product.audience),
    cta: irresistibleOffer(product),
    proof: gatherTestimonials(),
  };
}

// deploy &amp;&amp; measure &amp;&amp; iterate
const results = await abTest(pageA, pageB);
const winner = results.conversionRate &gt; 0.03
  ? results.variant
  : optimize(results);</pre>`;
        el.style.opacity = '0.03';
      }
    });
  });
</script>
