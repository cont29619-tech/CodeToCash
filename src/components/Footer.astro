---
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/drm-101', label: 'DRM 101' },
  { href: '/playbooks', label: 'Playbooks' },
  { href: '/blog', label: 'Blog' },
];

const resourceLinks = [
  { href: '/tools', label: 'Tools' },
  { href: '/newsletter', label: 'Newsletter' },
  { href: '/about', label: 'About' },
  { href: 'mailto:hello@codetocash.dev', label: 'Contact' },
];
---

<footer class="bg-navy-light border-t border-white/5">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-10">
      <!-- Brand -->
      <div class="md:col-span-1">
        <a href="/" class="text-xl font-bold text-white tracking-tight">
          CodeToCash<span class="text-coral">.dev</span>
        </a>
        <p class="mt-3 text-sm text-gray-400 leading-relaxed">
          Direct response marketing education for vibe coders and indie developers.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-sm font-semibold text-white uppercase tracking-wider mb-4">Navigation</h3>
        <ul class="space-y-2">
          {navLinks.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-gray-400 hover:text-coral transition-colors duration-200">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Resources -->
      <div>
        <h3 class="text-sm font-semibold text-white uppercase tracking-wider mb-4">Resources</h3>
        <ul class="space-y-2">
          {resourceLinks.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-gray-400 hover:text-coral transition-colors duration-200">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Connect + Email Signup -->
      <div>
        <h3 class="text-sm font-semibold text-white uppercase tracking-wider mb-4">Connect</h3>
        <ul class="space-y-2 mb-6">
          <li><a href="#" class="text-sm text-gray-400 hover:text-coral transition-colors duration-200">Twitter / X</a></li>
          <li><a href="#" class="text-sm text-gray-400 hover:text-coral transition-colors duration-200">GitHub</a></li>
          <li><a href="#" class="text-sm text-gray-400 hover:text-coral transition-colors duration-200">YouTube</a></li>
        </ul>

        <h3 class="text-sm font-semibold text-white uppercase tracking-wider mb-3">Stay in the loop</h3>
        <form action="https://buttondown.com/api/emails/embed-subscribe/CodeToCash" method="post" target="_blank" class="flex gap-2">
          <input type="hidden" value="1" name="embed" />
          <input
            type="email"
            name="email"
            placeholder="you@email.com"
            aria-label="Email address"
            class="flex-1 min-w-0 px-3 py-2 bg-white/5 border border-white/10 rounded-lg text-sm text-white placeholder-gray-500 focus:outline-none focus:border-coral/50 focus:ring-1 focus:ring-coral/50 transition-colors duration-200"
          />
          <button
            type="submit"
            class="px-4 py-2 bg-coral text-white text-sm font-semibold rounded-lg hover:bg-coral/90 transition-colors duration-200 shrink-0"
          >
            Join
          </button>
        </form>
      </div>
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-white/5">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <p class="text-center text-xs text-gray-500">
        &copy; 2026 CodeToCash.dev &mdash; Built for vibe coders who want to sell what they ship.
      </p>
    </div>
  </div>
</footer>
